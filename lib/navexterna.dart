import 'package:flutter/material.dart';

class navExterna extends StatefulWidget{
  const navExterna({super.key});

  @override

  State<navExterna> createState() => _navExternaState();
}

class _navExternaState extends State<navExterna>{

  @override
  Widget build(BuildContext context) {

    return SingleChildScrollView(
      child: Center(
        child: Column(
      ),
    ),
  );
  
  }

}